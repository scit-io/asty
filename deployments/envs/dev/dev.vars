# deployments/envs/dev/dev.vars
#
# Secrets and per-service tunables for local dev.
# Asty itself reads deployments/envs/dev/config.asty; what stays here
# is what either belongs in env (secrets) or feeds the demo x-services
# (their .asty files reference $X_*).
#
# Loaded by start.sh, which exports every K=V line into the asty
# server/agent processes.

# NATS auth (empty in dev)
A_NATS_USER     = ""
A_NATS_PASSWORD = ""

# Asty token (kept here so it's not in a checked-in YAML)
A_TOKEN         = "dev-token-secret"

# Asty host hardware override — embedded NATS dev container reports
# values that don't match the host; pin them so the scheduler has
# stable capacity for tests.
A_CPU_TOTAL     = 2200
A_MEMORY_TOTAL  = 466

# xauth (demo)
X_AUTH_USERNAME       = "admin"
X_AUTH_PASSWORD       = "password"
X_AUTH_ACCESS_SECRET  = "dev-access-secret"
X_AUTH_REFRESH_SECRET = "dev-refresh-secret"
X_AUTH_COOKIE_DOMAIN  = ""
X_AUTH_COOKIE_SECURE  = "false"
X_AUTH_COOKIE_SAMESITE = "lax"
X_AUTH_ACCESS_TTL     = "15m"
X_AUTH_REFRESH_TTL    = "168h"

# xhttp (demo)
X_HTTP_DATABASE_URL = "postgres://platform:platform@localhost:5432/platform?sslmode=disable"
X_HTTP_CACHE_TTL    = "30s"

# xws (demo)
X_WS_INACTIVITY_TIMEOUT = "3m"
