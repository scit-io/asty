# По умолчанию фронт ходит относительными путями через Vite dev-proxy
# на http://127.0.0.1:80 (Asty dev-node-1 gateway). Раскомментируй и задай
# абсолютный URL, если хочешь обращаться к Gateway напрямую (потребует CORS).
# VITE_GATEWAY_URL=http://127.0.0.1
